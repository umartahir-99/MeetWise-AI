{
  "permissions": {
    "allow": [
      "PowerShell(npx tsc -b; if \\($?\\) { \"TSC OK\" }; npx oxlint src/components/LiveCapture.tsx src/useLiveRecorder.ts src/App.tsx)",
      "PowerShell(npx tsc -b; if \\($?\\) { \"TSC OK\" }; npx oxlint src/components/LiveCapture.tsx src/useLiveRecorder.ts src/App.tsx; npx vite build 2>&1 | Select-Object -Last 4)",
      "PowerShell(npx tsc -b; if \\($?\\) { \"TSC OK\" }; npx oxlint src/App.tsx src/components/Processing.tsx src/components/LiveCapture.tsx src/useLiveRecorder.ts)",
      "PowerShell(npx tsc -b; if \\($?\\) { \"TSC OK\" }; npx oxlint src/mockData.ts src/components/LiveCapture.tsx src/useLiveRecorder.ts)",
      "PowerShell(npx tsc -b; if \\($?\\) { \"TSC OK\" }; npx oxlint src/components/Upload.tsx)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.argv[1];const f=require('fs');if(f.existsSync(p)){const r=require('child_process').spawnSync(process.execPath,[p],{stdio:'inherit'});process.exit(r.status===null?1:r.status);}\" \"${CLAUDE_PROJECT_DIR}/.claude/skills/impeccable/scripts/hook.mjs\"",
            "timeout": 5,
            "statusMessage": "Checking UI changes"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.argv[1];const f=require('fs');if(f.existsSync(p)){const r=require('child_process').spawnSync(process.execPath,[p],{stdio:'inherit'});process.exit(r.status===null?1:r.status);}\" \"${CLAUDE_PROJECT_DIR}/.claude/skills/impeccable/scripts/hook.mjs\"",
            "timeout": 30,
            "statusMessage": "Design deep pass"
          }
        ]
      }
    ]
  },
  "description": "Impeccable design detector: immediate-tier checks after Edit/Write on UI files, full-rule deep pass on Stop."
}
